#include "main.h"

/**
 * main - Entry point for program to prints it name
 * @argc: counter of argv passed in
 * @argv: string passed in
 * Return: 0
 */

int main(int argc, char *argv[])
{
	/*for (i = 0; argc >= i; i++)*/
		if (argc == 1)
		{
			printf("%s\n", argv[0]);
		}
	return (0);
}
