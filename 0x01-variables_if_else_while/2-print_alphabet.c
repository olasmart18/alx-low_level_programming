#include <stdio.h>
/**
* main - Entry point
* the code prints alphabet to std output in lowecase
* Return: Always 0 (Success)
*/

int main(void)
{
int i;
for (i = 'a'; i <= 'z'; i++)
{
	putchar(i);
}
	putchar('\n');
return (0);
}
