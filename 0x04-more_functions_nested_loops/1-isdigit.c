#include "main.h"

/**
* _isdigit - the function check for a digit number
* @c: the digit input
* Return: 1 if its digit, 0 if its not digit
*/

int _isdigit(int c)

{
	if (c != 'a')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
